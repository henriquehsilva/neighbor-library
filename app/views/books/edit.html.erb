<h1>Retirar/Devolver Livro</h1>
<p>
  Para retirar o livro basta preencher o campo com o nome de quem está pegando emprestado.<br />
  Para devolver basta apagar o nome.
</p>
<%= form_with(model: @book, local: true) do |form| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label "Quem deseja retirar o livro #{@book.title} ?" %>
    <%= form.text_field :borrowed_to %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>


<%= link_to 'Show', @book %> |
<%= link_to 'Back', books_path %>
